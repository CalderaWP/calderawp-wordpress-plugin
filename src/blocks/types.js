export type GutenbergEditProps = {
	attributes: any,
	setAttributes: (any) => void,
	isSelected: boolean,
	className: string
};

export type GutenbergSaveProps = {
	attributes: any,
	className: string
};